<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>Blog</title>
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Lato:400,700,400italic,700italic" rel="stylesheet" type="text/css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <style>
        html, body {
            height: 100%; /* Asegura que el body ocupe toda la altura de la ventana */
            margin: 0; /* Elimina el margen predeterminado */
            background-color: #a8e6cf; /* Color verde tierno de fondo */
        }

        .wrapper {
            display: flex;
            flex-direction: column;
            min-height: 100vh; /* Asegura que el contenedor principal ocupe al menos la altura de la ventana */
        }

        .content {
          flex: 1; /* Esto permite que el contenido crezca y empuje el footer hacia abajo */
          margin: 20px; /* Agrega margen alrededor del contenido */
          margin-top: 140px;
          margin-bottom: 140px;
          background-color: #a8e6cf; /* Color verde tierno de fondo */
          border-radius: 10px; /* Opcional: añade bordes redondeados */
          padding: 20px; /* Opcional: agrega un poco de padding interno */
      }
      

        footer {
            padding: 20px 0; /* Espaciado adicional para el footer */
            background-color: #f8f9fa; /* Fondo del footer (opcional) */
        }
    </style>
</head>

<body id="page-top">
    <div class="wrapper">
        <!-- Navigation -->
        <nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
            <div class="container">
                <a class="navbar-brand js-scroll-trigger" href="/">Blog SAAS</a>
                <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                    Menu
                    <i class="fa fa-bars"></i>
                </button>
                <div class="collapse navbar-collapse" id="navbarResponsive">
                    <ul class="navbar-nav ml-auto">
                        <% if user_signed_in? %>
                            <li class="nav-item">
                                <span class="nav-link"><%= current_user.email %></span> <!-- Muestra el nombre del usuario -->
                            </li>
                            <li class="nav-item">
                                <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'nav-link js-scroll-trigger' %> <!-- Botón de logout -->
                            </li>
                        <% else %>
                            <li class="nav-item">
                                <%= link_to 'Login', new_user_session_path, class: 'nav-link js-scroll-trigger' %> <!-- Opción de Login -->
                            </li>
                            <li class="nav-item">
                                <%= link_to 'Register', new_user_registration_path, class: 'nav-link js-scroll-trigger' %> <!-- Opción de Registro -->
                            </li>
                        <% end %>
                            <li class="nav-item">
                                <%= link_to 'My Blog', articles_path, class: 'nav-link js-scroll-trigger' %>
                            </li>

                    </ul>
                </div>
            </div>
        </nav>

        <!-- Main Content -->
        <div class="content">
            <%= yield %>
        </div>

        <!-- Footer -->
        <footer class="text-center">
            <div class="footer-below">
                <div class="container">
                    <div class="col-lg-12">
                        <h3>Ingeniería en Telemática</h3>
                        <ul class="list-inline">
                            <li class="list-inline-item">
                                <a class="btn-social btn-outline" href="#">
                                    <i class="fa fa-fw fa-facebook"></i>
                                </a>
                            </li>
                            <li class="list-inline-item">
                                <a class="btn-social btn-outline" href="#">
                                    <i class="fa fa-fw fa-twitter"></i>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </footer>
    </div>

    <!-- Scroll to Top Button (Only visible on small and extra-small screen sizes) -->
    <div class="scroll-top d-lg-none">
        <a class="btn btn-primary js-scroll-trigger" href="">
            <i class="fa fa-chevron-up"></i>
        </a>
    </div>

    <!-- Bootstrap core JavaScript -->
    <script src="assets/jquery.min.js"></script>
    <script src="assets/bootstrap.bundle.min.js"></script>
    <!-- Contact Form JavaScript -->
    <script src="assets/jqBootstrapValidation.js"></script>
    <!-- Custom scripts for this template -->
    <script src="assets/freelancer.min.js"></script>
</body>
</html>
